name: Test and Build

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  schedule:
    - cron: '0 0 * * 0' # Run weekly

jobs:
  test-formulas:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set up Homebrew
        run: |
          brew update
          
      - name: Tap repository
        run: |
          mkdir -p "$(brew --repo)/Library/Taps/jrrk2"
          ln -s "$PWD" "$(brew --repo)/Library/Taps/jrrk2/homebrew-kstars"
          
      - name: Test INDI formula
        run: brew audit --strict jrrk2/kstars/indi-lib
        
      - name: Test KStars formula
        run: brew audit --strict jrrk2/kstars/kstars
        
  build-bottles:
    needs: test-formulas
    runs-on: macos-latest
    strategy:
      matrix:
        formula: [indi-lib, kstars]
        
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set up Homebrew
        run: |
          brew update
          
      - name: Tap repository
        run: |
          mkdir -p "$(brew --repo)/Library/Taps/jrrk2"
          ln -s "$PWD" "$(brew --repo)/Library/Taps/jrrk2/homebrew-kstars"
          
      - name: Install dependencies
        run: |
          brew install --only-dependencies jrrk2/kstars/${{ matrix.formula }}
          
      - name: Build bottle
        run: |
          brew install --build-bottle jrrk2/kstars/${{ matrix.formula }}
          brew bottle jrrk2/kstars/${{ matrix.formula }}
          
      - name: Upload bottle
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.formula }}-bottle
          path: ./${{ matrix.formula }}*.bottle.tar.gz
          retention-days: 7
